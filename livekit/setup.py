from setuptools import setup, find_packages

setup(
    name="livekit-agents",
    version="0.1.0",
    packages=find_packages(),
    include_package_data=True,
    description="Custom LiveKit voice agents for travel assistant",
)
